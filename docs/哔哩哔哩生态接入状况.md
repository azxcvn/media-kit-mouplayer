# 哔哩哔哩生态接入状况

> 本文件按「工作.md 第 7 点」约定维护：每完成一个阶段后更新，记录已完成 / 未完成 / 风险与待办。
> 阶段划分对应「工作.md」四阶段（登录 → 番剧索引 → 番剧播放 → 下载），并参考
> `杂项文件/bili-ecosystem-research/` 调研报告的 P0–P5 实施计划。

---

## 阶段一：账户登录 ✅ 已完成

### 已完成内容

1. **基础设施**
   - 新增 `qr_flutter`（二维码渲染，纯 Dart）与 `flutter_secure_storage`（Cookie 加密存储，
     Android 走 EncryptedSharedPreferences/Keystore，对齐小喵 player 方案）两个依赖。
   - `lib/services/bilibili/bili_constants.dart`：域名 / 固定 UA / Referer / TV appkey·appsec / 扫码状态码。
   - `lib/services/bilibili/bili_api.dart`：端点常量（集中式，对齐 PiliPlus `api.dart`）。
   - `lib/utils/bili_wbi.dart`：WBI 签名纯函数（`biliGetMixinKey` / `biliEncWbi`，
     混淆表以 bilibili-API-collect / Bili23 的 64 项表为准，官方示例向量已验证）。
   - `lib/services/bilibili/bili_http.dart`：统一请求封装（Cookie/UA/Referer/buvid3 注入 +
     UTF-8 解码 + 错误语义化 `BiliApiException`）。

2. **凭证安全存储**
   - `lib/services/bilibili/bili_credential_store.dart`：SESSDATA / bili_jct / DedeUserID +
     access_token / refresh_token 加密存储；`BiliCredential` 提供 Cookie 解析（cookie_info.cookies
     的 `fromCookies` / 浏览器复制 Cookie 的 `parse` 两种来源）。

3. **扫码登录（对齐 PiliPlus 的 TV 通道）**
   - `lib/utils/bili_app_sign.dart`：TV 端 appSign（appkey/appsec MD5，对齐 PiliPlus）。
   - `lib/services/bilibili/bili_auth_service.dart`：TV 扫码生成/轮询（android_hd + appSign，
     凭证直接 JSON 返回 token_info + cookie_info，**不依赖 Set-Cookie**）/ nav 自检 / buvid 预取 / 退出登录。
   - `lib/pages/bilibili/bili_login_page.dart`：扫码页（二维码 + 状态提示 + 180 秒倒计时自动刷新）
     + Cookie 导入页（粘贴解析兜底）。
   - 扫码页三个操作：**刷新二维码** / **保存到相册**（RepaintBoundary 截图 + `saver_gallery`）/
     **打开哔哩哔哩**（`bilibili://browser` 深链直接 `startActivity`，未装抛异常提示未安装）。
   - 原生侧：`MainActivity.kt` 新增 `openBilibiliScan`；`AndroidManifest.xml` 新增 `bilibili://` 的 `<queries>` 声明。

4. **登录态管理与入口**
   - `lib/services/bilibili/bili_account.dart`（ChangeNotifier 单例）：登录态 / 用户信息 /
     WBI 密钥缓存 / buvid；启动 `ensureLoaded` 读凭证 + nav 自检（Cookie 过期静默清凭证）。
   - 「我的」页账号入口：未登录显示「登录 / 哔哩哔哩账号」，登录后显示头像/昵称/会员状态，
     点击弹确认退出；`main.dart` 启动时 `ensureLoaded`。

### 新增 / 修改文件

| 类型 | 文件 |
|---|---|
| 模型 | `lib/models/bilibili_user.dart` |
| 纯函数 | `lib/utils/bili_wbi.dart`、`lib/utils/bili_app_sign.dart` |
| 服务 | `lib/services/bilibili/bili_constants.dart` / `bili_api.dart` / `bili_credential_store.dart` / `bili_http.dart` / `bili_auth_service.dart` / `bili_account.dart` |
| 页面 | `lib/pages/bilibili/bili_login_page.dart`、`lib/pages/bilibili/bili_user_page.dart` |
| 原生 | `android/.../MainActivity.kt`、`android/.../AndroidManifest.xml` |
| 接入 | `lib/pages/settings/settings_page.dart`、`lib/main.dart`、`lib/services/device_services.dart`、`pubspec.yaml` |
| 测试 | `test/bili_wbi_test.dart`、`test/bili_app_sign_test.dart`、`test/bili_credential_test.dart`、`test/bilibili_user_test.dart` |

### 验证状态

- `flutter analyze` 通过（新增文件零告警）。
- `flutter test` 全绿（613 项，含新增 WBI / appSign / 凭证解析 / 存储 / 用户模型测试）。
- **未做真机联调**（按约定由用户在 Android Studio 真机编译测试）。

### 已知待办 / 风险（阶段一遗留）

- **TV 通道风险**：扫码登录已用 TV 通道（android_hd 第三方 appkey，灰色地带），PiliPlus 在设置里
  明示风险；本项目后续可在设置页加醒目提示。
- **反爬指纹是精简版**：当前只做 buvid3 预取 + 固定 UA + Referer。Bili23 的完整反爬
  （buvid_fp / b_lsid / b_nut / bili_ticket via GenWebTicket / ExClimbWuzhi）未接入——
  这些主要影响 **playurl 取流**（阶段三），若届时遇 412 风控再补。
- **Cookie 过期提示**：nav 自检发现过期目前是「静默清凭证回游客态」，未做「非阻塞提示重新扫码」。
- **退出登录**调用了 `/login/exit/v2` 但未严格验证其返回（本地清凭证为准）。

---

## 阶段二：番剧索引页（索引 / 搜索 / 详情）⏳ 未开始

计划要点（详见调研报告 P4）：`/pgc/season/index/condition`（筛选维度）→
`/pgc/season/index/result`（分页列表）→ `/pgc/view/web/season`（季详情 episodes）→
`/pgc/web/timeline`（新番时间表）；入口复用首页速拨「哔哩番剧」。

## 阶段三：番剧播放（播放 / 弹幕 / 画质）⏳ 未开始

计划要点（调研报告 P2+P3）：UGC `/x/player/wbi/playurl` + PGC `/pgc/player/web/v2/playurl`
（fnval=4048 DASH）→ mpv 双流播放（`audio-file` 外挂音轨 + `http-header-fields` 注入 Referer/UA）；
分段弹幕 `/x/v2/dm/web/view` + `/x/v2/dm/web/seg.so`（手写 protobuf 解码）；「更多」新增「画质」。

## 阶段四：弹幕 / 视频下载 ⏳ 未开始（待商榷）

计划要点（调研报告 P5）：分块并发 + Range 续传 + ffmpeg_kit 合并；下载产物同名落盘
`{标题}.xml` 弹幕，复用本地媒体库「9 种命名规则」自动加载。
